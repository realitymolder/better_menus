import 'package:flutter/material.dart';

class HorizontalMenuPage extends StatelessWidget {
  const HorizontalMenuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('');
  }
}
